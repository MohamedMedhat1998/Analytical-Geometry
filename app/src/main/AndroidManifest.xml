<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andalus.abo_med7at.analyticalgeometry">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PairActivity" />
        <activity android:name=".Drawing_class" />
        <activity android:name=".CircleActivity" />
        <activity android:name=".ParabolaActivity" />
        <activity android:name=".EllipseActivity" />
        <activity android:name=".HyperbolaActivity" />
        <activity android:name=".GeneralActivity" />
    </application>

</manifest>